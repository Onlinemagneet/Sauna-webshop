{% comment %}
  Benefit Pillars Section - 4 pijlers
{% endcomment %}

<section class="benefit-pillars">
  <div class="container">
    <div class="text-center mb-4">
      <h2 class="h2">Waarom een sauna?</h2>
      <p class="benefit-pillars__subheading">De wetenschappelijk bewezen voordelen van regelmatig saunagebruik</p>
    </div>
    
    <div class="benefit-pillars__grid">
      <div class="pillar">
        <div class="pillar__icon">💪</div>
        <h3 class="pillar__title">Spierherstel</h3>
        <p class="pillar__description">Verlicht gespannen spieren en versnelt herstel na training.</p>
        <div class="pillar__benefit">Tot 40% sneller herstel</div>
      </div>
      
      <div class="pillar">
        <div class="pillar__icon">😴</div>
        <h3 class="pillar__title">Betere slaap</h3>
        <p class="pillar__description">Diepe ontspanning zorgt voor een rustigere nachtrust.</p>
        <div class="pillar__benefit">Dieper slapen</div>
      </div>
      
      <div class="pillar">
        <div class="pillar__icon">🧘</div>
        <h3 class="pillar__title">Stressreductie</h3>
        <p class="pillar__description">Moment van rust en mindfulness in je eigen huis.</p>
        <div class="pillar__benefit">Minder stress</div>
      </div>
      
      <div class="pillar">
        <div class="pillar__icon">❤️</div>
        <h3 class="pillar__title">Hartgezondheid</h3>
        <p class="pillar__description">Verbetert bloedsomloop en hartfunctie.</p>
        <div class="pillar__benefit">Gezonder hart</div>
      </div>
    </div>
  </div>
</section>

<style>
.benefit-pillars {
  padding: 4rem 0;
  background: white;
}

.benefit-pillars__subheading {
  font-size: 1.125rem;
  color: var(--muted);
  margin-top: 1rem;
}

.benefit-pillars__grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 2rem;
  margin-top: 3rem;
}

.pillar {
  text-align: center;
  padding: 2rem 1rem;
  border-radius: var(--radius);
  transition: transform 0.2s ease;
}

.pillar:hover {
  transform: translateY(-4px);
}

.pillar__icon {
  width: 4rem;
  height: 4rem;
  margin: 0 auto 1.5rem;
  background: var(--accent);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.5rem;
  color: white;
}

.pillar__title {
  font-size: 1.25rem;
  font-weight: 700;
  margin-bottom: 1rem;
  color: var(--primary);
}

.pillar__description {
  color: var(--muted);
  line-height: 1.6;
  margin-bottom: 1rem;
}

.pillar__benefit {
  font-weight: 600;
  color: var(--accent);
  font-size: 0.875rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

@media (max-width: 768px) {
  .benefit-pillars {
    padding: 2rem 0;
  }
  
  .benefit-pillars__grid {
    grid-template-columns: 1fr;
    gap: 1.5rem;
    margin-top: 2rem;
  }
  
  .pillar {
    padding: 1.5rem 1rem;
  }
}
</style>
